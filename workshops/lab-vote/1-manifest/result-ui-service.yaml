apiVersion: v1
kind: Service
metadata:
  labels:
    app: result-ui
  name: result-ui
spec:
  type: LoadBalancer
  ports:
    - name: "result-ui-service"
      port: 80
      targetPort: 80
  selector:
    app: result-ui
