apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  database.properties: |
    host=mysql-service
    port=3306
    database=myapp
  nginx.conf: |
    server {
      listen 80;
      location / {
        proxy_pass http://localhost;
      }
    }